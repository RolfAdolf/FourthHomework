syms 'x';
y = exp(1/(1-x^2))/(1+x^2);
F = @(x)exp(1./(1-x.^2))./(1+x.^2);
ezplot(y, -7, 7);
syms 'x';
y = exp(1/(1-x^2))/(1+x^2);
hold on;
ezplot(y, -7, 7);
plot([-1 -1], [-20 20])
plot([1 1], [-20 20])
dify = diff(y)
minmax = solve(dify);
plot(minmax, F(minmax), 'o');
